You are working on the Medical Reasoning (MedXpertQA) — Local Solver prompt-engineering task in team_files/prompt_engineering/medical_reasoning_local/.

Goal: improve the system prompt that drives a LOCAL solver model (Qwen3.5-27B, served on a remote GPU via an SSH tunnel opened on the host) on medical multiple-choice questions. Edit only the prompt; maximize dev accuracy (n=20) and report the held-out test set (n=100), selecting by dev only. The solver needs QWEN_BASE_URL pointing at the tunnel — from inside the Docker sandbox that means the host gateway, http://host.docker.internal:8010/v1 (the default), not 127.0.0.1. The tunnel's port can change between sessions: if the default 8010 isn't reachable, don't assume it — poke around with `python check_tunnel.py` (it scans the likely host:port combos, e.g. 8111, and prints a working base URL), then export QWEN_BASE_URL to whatever it finds.

You are the prompt engineer, and this is a by-hand loop: score a prompt, read the per-question failures, revise the system prompt, and re-score — iterating for many rounds until dev accuracy plateaus. Use `python run_iter.py <prompt_file>` for each iteration (it scores dev, scores test on the report cadence, updates the live dashboard, and prints the questions still being missed so you know what to fix next); `eval_qwen.py` is the underlying frozen scorer for a one-off check. Read README.md and task.json in that folder for the full workflow, then carry out the task as described there.
