You are working on the Medical Reasoning (MedXpertQA) — Local Solver prompt-engineering task in team_files/prompt_engineering/medical_reasoning_local/.

Goal: improve the system prompt that drives a LOCAL solver model (Qwen3.5-27B, served on a remote GPU via an SSH tunnel opened on the host) on medical multiple-choice questions. Edit only the prompt; maximize dev accuracy (n=20) and report the held-out test set (n=100), selecting by dev only. The solver needs QWEN_BASE_URL pointing at the tunnel — from inside the Docker sandbox that means the host gateway, http://host.docker.internal:8111/v1 (the default), not 127.0.0.1. The tunnel's port can change between sessions: if the default 8111 isn't reachable, don't assume it — poke around with `python check_tunnel.py` (it scans the likely host:port combos and prints a working base URL), then export QWEN_BASE_URL to whatever it finds.

IMPORTANT — the system prompt must be a GENERAL reasoning strategy, not an answer key. `run_iter.py` prints the dev misses (with gold labels) so you can diagnose *why the reasoning failed* and fix it generally — do NOT hardcode or paste the answers to specific dev questions into the prompt (e.g. "for the trauma/FAST question the answer is E"). A prompt that memorizes dev-specific answers will spike dev accuracy but will NOT transfer to the held-out test, which is the number that counts. Keep the prompt about clinical reasoning, formatting, and decision rules that apply to any question.

You are the prompt engineer, and this is a by-hand loop with a CRISP four-step protocol: (1) FIRST seal the baseline's held-out number: `python submit.py --baseline` (a one-shot, aggregate-only test eval of the shipped baseline_prompt.txt). (2) Then iterate on dev EXACTLY 20 times: each round, `python run_iter.py <prompt_file>` scores dev (n=20), updates the live dashboard, and prints the questions still being missed — read the misses, revise the system prompt, and run the next iteration; selection is by dev ONLY, and the held-out test set is OFF-LIMITS during iteration (`eval_qwen.py` is the underlying frozen scorer). (3) After iteration 20, submit the dev-best prompt: `python submit.py --prompt-file <best_prompt_file>` (the second and last sealed test eval). (4) Write `RESULTS.md` in the task folder: baseline vs best dev accuracy, the two sealed TEST numbers (baseline vs final — the delta shows whether your prompt engineering transferred), which prompt ideas helped, and the dev questions still missed. Read README.md and task.json in that folder for the full workflow, then carry out the task as described there.
